# Public config for the TruthCheck website's Supabase project, shared so the
# extension signs in with the same accounts as the site. Both values are public
# by design; there are no other secrets in this repo.
#
# The extension has no build step, so it reads these from extension/config.js
# (supabaseUrl / supabaseKey). Keep the two in sync if either changes.
VITE_TRUTHCHECK_SUPABASE_URL=https://c--18989893-2e9e-4c1d-af2b-9d5cc74c2c93-prod.lovable.cloud
VITE_TRUTHCHECK_SUPABASE_KEY=sb_publishable_f63_GjfD38ZrafUKLupvuQ_Ekwv7yy_
